# Sky Work v5.9 — หน้างบประมาณ ทวนสอบที่มาได้แล้ว

## Deploy
อัปทุกไฟล์ทับ → Commit → เปิดแอป → แตะโลโก้ Sky Work → ต้องขึ้น "เวอร์ชัน 5.9"

## ตัวเลขในหน้างบมาจากไหน (เขียนไว้ในหน้าจอแล้ว)
งบที่ตั้งไว้ = ค่าที่ "มากกว่า" ระหว่าง
  (ก) งบตั้งต้นที่ตั้งเองใน ตั้งค่า → หมวด GL (แยกรายปี)
  (ข) ผลรวมช่อง "งบ" ของทุกงานที่แท็กหมวดนี้ในปีนั้น
  ใช้ค่าที่มากกว่า ไม่บวกกัน เพื่อไม่ให้นับซ้ำ

ใช้จริง = ผลรวมช่อง "ใช้จริง" ของงานในหมวดนี้
        + รายจ่ายที่บันทึกด้วยปุ่ม "บันทึกเงิน" และแท็กหมวดเดียวกัน

## แตะแถวได้แล้ว
แตะแถวหมวด GL ไหนก็ได้ จะขึ้นกล่องแจกแจงว่าเลขนั้นมาจากอะไร
- งบตั้งต้นที่ตั้งเอง (ถ้ามี)
- งานทุกรายการในหมวดนั้น พร้อมงบและใช้จริงของแต่ละงาน
- รายการเงินที่บันทึกไว้ในหมวดนั้น
แตะรายการในกล่อง = เปิดแก้ไขงานหรือรายการเงินนั้นทันที
หัวแถวบอกจำนวนงานและรายการเงินไว้ด้วย
